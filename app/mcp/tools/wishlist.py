# TODO: Wishlist tools
# add_to_wishlist(user_id, product_id) -> None
# remove_from_wishlist(user_id, product_id) -> None
# get_wishlist(user_id) -> list[Product]
from fastapi import APIRouter
router = APIRouter()
