// Implement in Week 3 (Days 15–16)
// Admin-only NL-to-SQL panel — connects to POST /api/analytics/query.
// Displays: natural language input, generated SQL, result table, insight text.
export default function AdminDashboard() {
  return <div>AdminDashboard</div>;
}
