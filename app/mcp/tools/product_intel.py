# TODO: Product intelligence tools
# get_frequently_bought_together(product_id) -> list[Product]
# get_compatible_accessories(product_id) -> list[Product]
from fastapi import APIRouter
router = APIRouter()
