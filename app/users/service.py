# Business logic — implement in Week 1 (Day 6):
#   create_user()         — hash password with passlib, INSERT to users
#   authenticate_user()   — verify password, return JWT via python-jose
#   get_preferences()     — read user_preferences row for given user_id
#   update_preferences()  — called ONLY by personalisation worker, never by user directly
