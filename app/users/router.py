from fastapi import APIRouter

router = APIRouter()

# Endpoints — implement in Week 1 (Day 6):
#   POST /register          — create account
#   POST /login             — returns JWT
#   GET  /me                — current user profile
#   GET  /me/preferences    — preference profile (written by worker, read by agent)
