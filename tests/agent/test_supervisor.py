# TODO: Tests for the supervisor node (was test_classify_intent.py)
# Rename the existing tests; add routing assertion tests for new intents:
# - VISUAL_SEARCH → visual_search node
# - POLICY_QUERY  → policy_rag node
# - CLARIFY       → clarifying_question node
import pytest
